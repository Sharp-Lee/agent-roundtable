# Decisions log — roundtable v2「前置设计流程」(玄/素)

> Resolved disagreements and the reasoning behind them. Keeps the roundtable from
> re-litigating settled points and gives a restarted pane the "why" behind choices.
> **Reset 2026-06-22** for the v2 pipeline build; prior entries (tmux-workbench project)
> are retired — see git history / `channel.md` if needed.

| # | Question / disagreement | Decision | Rationale | Decided by | Phase |
|---|-------------------------|----------|-----------|------------|-------|
| 1 | 构建顺序:按"关注点"(G1..G12)逐组改,还是按物理 sink 分批? | **按物理 sink 分批**:Batch A = G9 模板(architecture/flow 新增 + requirements 升级,隔离);Batch B = `protocol.md` 重写 + `lead.md→xuan.md`/`impl.md→su.md` + `bin/roundtable` 接线,**一次原子提交**;Batch C = README 中/英 + usage/banner/kickoff 文字补齐;Batch D = 一致性自检 + 全量验证。 | 12 组写入高度集中在 `protocol.md` + 两份角色契约;逐组各改一遍再到 G11 重写 = 反复打架。sink-分批一次写成,避免中间不一致。 | lead+impl | 2-build |
| 2 | 角色改名 玄/素 在 `bin/roundtable` 的范围:shape (a) 纯保留 lead/impl 内部 token,还是 (b) 全量改名? | **shape (a+)**:wire/control-plane token 保留 `lead`/`impl`(mailbox 文件名、`FROM:` wire 值、`LEAD_PANE`/`IMPL_PANE`、relay routes、内部变量、兼容 CLI 参数);display/contract token 切 `玄`/`素`(`xuan.md`/`su.md`、kickoff 正文、README、usage/banner、pane title)。新增 `roundtable kickoff xuan|su` 别名,`lead|impl` 保留为 legacy 别名。R-EX-3 一致性检查用**文档化的 wire/compat allowlist**,不要求全仓零 `lead/impl`。 | 纯 (a) 错在 lead/impl 其实用户可见(kickoff/pane title/help);纯 (b) 会牵动 relay/env/mailbox 文件名/selftest,越过"relay 阶段无关、引擎极小"边界。a+ 在两者间取正确切面。 | lead+impl | 2-build |
| 3 | 契约改名与引擎接线能否分到不同 commit/批次? | **不能**:`prompts/lead.md`/`impl.md` 一旦 `git mv`,而 `cmd_init` 仍复制旧文件、`_kickoff_text` 仍指旧文件,当前 checkout 在 `init/start/kickoff` 即破。`protocol.md` 重写 + 两份角色契约 rename + `bin/roundtable`(contract 路径/kickoff/init/selftest)**同一原子提交**落地;两份角色契约也不拆两个 commit(与 protocol 同一语义变更)。 | 这是可运行性而非审美;中间 commit 破坏 `roundtable start`。 | lead+impl | 2-build |
| 4 | 落点 B 的精确边界:requirements/decisions 是否随 R-F-5 迁出 `.roundtable/`? | **honor 已锁的 R-F-5**:设计产物 `architecture.md`/`flow.md`/`requirements.md`/`decisions.md` → tracked **`docs/design/roundtable/`**(命名空间化,避免与项目自有 `docs/design/*` 冲突);交接态 `channel.md`/`_idea.md` + mailbox/panes/kickoff 留 `.roundtable/`。requirements/decisions 的路径迁移作为 **Batch B 内显式、带验收+selftest 的迁移项**,不是 G9 静默子条目。**relay.py 不受影响**(relay 只搬 mailbox + append channel)。 | R-F-5 是关卡 2 已锁基线,明确把四份都列为设计产物 → docs/;lead 不单方面松动已锁需求。素的有效诉求(命名空间 + 显式验收)被采纳;若素发现具体恢复正确性缺陷(非品类偏好),按 R-G2-3 升级 arbiter,而非私自改基线。 | lead+impl | 2-build |
| 5 | R-P0-3 的 HOW 草案写了 `STATUS=gate` mailbox 状态 | **不新增 mailbox 状态**:玄/素 mailbox 仍只用 `needs-reply/agreed/blocked/escalate`。关卡请求 = 玄在自己 pane 输出里向 arbiter 呈现(arbiter 不经 mailbox),关卡裁决落 `decisions.md`。 | 新增状态会破坏 protocol 状态枚举和"读到未知 STATUS 就不要猜"的恢复规则;HOW 是 agent 自有,改它不动已锁 WHAT。 | lead+impl | 2-build |
| 6 | 契约改名后 relay nudge 路径(`relay.py:118` 发 `act per .roundtable/prompts/{lead\|impl}.md`)会指向被删文件 | **允许 relay.py 一处 display-only 改动**:nudge 字符串按 wire token → 契约文件名映射(`lead→xuan`,`impl→su`),发 `.roundtable/prompts/xuan.md`/`su.md`;**routing(mailbox→pane)与 route 键仍 `lead`/`impl` 不变**;`.roundtable/prompts/` 单一来源(只 `protocol/xuan/su`,**不生成 lead.md/impl.md 别名**)。修正 decisions #2 的"relay 零改动"措辞为"几乎不改:仅 nudge 显示串"。 | 生成 lead.md/impl.md 别名 = 双权威/漂移隐患(同上轮 stale-binding bug);relay 仍阶段无关,§4 是"几乎不改"非"零改";nudge 改后正确指向 agent 真实契约名。 | lead+impl | 2-build |
| 7 | req/dec 迁 docs 的安全序(尤其本 dogfooding 会话的活控制面) | **copy-forward + 冻结 legacy mirror + 恢复 fallback**:Batch B 内把活 `.roundtable/{requirements,decisions}.md` copy-forward 到 tracked `docs/design/roundtable/`(`.roundtable/` 被 gitignore,故是 tracked copy 非 git mv);**copy 后 docs/ 为 lead 唯一维护权威,`.roundtable/` 旧副本冻结为 legacy mirror(不再更新)**;新契约/kickoff 恢复顺序 = docs/ primary +(旧项目缺失时)`.roundtable/` fallback 并迁移;`cmd_init` 新项目只在 docs/ 建 req/dec,不再在 `.roundtable/` 建主产物;selftest 覆盖。运行中的面板靠内存契约,新契约重启后生效(KTD-6)。 | docs/ 保持新鲜(lead 维护)+ `.roundtable/` 冻结,使 fallback 永不读到 stale primary;旧项目重启不断;避免双权威漂移。 | lead+impl | 2-build |
