# Decisions log

> Resolved disagreements and the reasoning behind them. Keeps the roundtable from
> re-litigating settled points and gives a restarted pane the "why" behind choices.

| # | Question / disagreement | Decision | Rationale | Decided by | Phase |
|---|-------------------------|----------|-----------|------------|-------|
| 1 | Edit scope: prompts-only or also adjacent files? | Prompts are primary; allow surgical 1-line touch-ups to templates/README/relay only where a prompt-only fix leaves a real contradiction. Each adjacent edit listed explicitly in requirements. | Some contradictions (e.g. channel.md ownership) span files; prompt-only fixes can resolve them but must be paired with the dependent file so no residual mismatch remains. | arbiter | 0 |
| 2 | How to resolve channel.md-ownership contradiction + where "agreement"/version live? | channel.md stays relay-owned (never hand-edited); remove lead's instruction to append AGREED there. Agreement is recorded by ticking the lead/impl sign-off boxes in requirements.md; the version lives in requirements.md and lead bumps it. | Keeps the machine transcript machine-owned, and moves human-meaningful state (sign-off, version) onto the committed artifact that is the source of truth. Resolves defects #1/#2/#3. | arbiter | 0 |
| 3 | Lead never commits, but lead authors artifact edits (sign-off, version, Gate-A tick, item `done`) — who commits them, and how are no-code items handled? | impl is the sole committer. Each lead-authored artifact edit is committed by impl on its next turn: it rides in the next code commit when code follows, and is a dedicated artifact-only commit (real diff, never `--allow-empty`) at the two no-code checkpoints — Phase-1 agreement and final item close — each triggered by a lead checkpoint-commit handoff. Commit set: requirements.md, decisions.md, channel.md; never the mailboxes, panes.env, or prompts/ copies. **(Superseded by #4 in v2.)** | Closes the turn-sequencing gap impl found: with lead never committing, lead's final sign-off/`done`/Gate-A edits had no turn to be persisted. Keeps "impl is sole implementer/committer" intact while guaranteeing on-disk state reaches git. | lead+impl | 1 |
| 4 | (v2 change request) Relax the commit model so lead persists its own doc edits instead of routing them through impl. | Lead MAY commit doc-only artifact changes (`requirements.md` / `decisions.md`, channel-free) directly. impl remains the sole implementer AND sole committer of CODE, and commits `channel.md` history with its code commits. The v1 checkpoint-commit handoffs and artifact-only checkpoint commits (#3) are removed. Core design intact: lead still never edits/commits code; impl is still the only implementer. | Simpler than #3 — no turn-sequencing machinery to persist lead's edits, no special-case no-code/closing commits. The arbiter judged doc-only commits by lead an acceptable relaxation since they never touch code. | arbiter | 1 |
